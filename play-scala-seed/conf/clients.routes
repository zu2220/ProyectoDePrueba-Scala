# Rutas para los clientes
GET     /clients        controllers.ClientController.getClients
+nocsrf
POST    /clients        controllers.ClientController.createClient
+nocsrf
PUT     /clients/:id    controllers.ClientController.editClient(id: String)
+nocsrf
DELETE  /clients/:id    controllers.ClientController.deleteClient(id: String)