# Rutas para los pedidos
GET     /orders        controllers.OrderController.getOrders
+nocsrf
POST    /orders        controllers.OrderController.createOrder
+nocsrf
PUT    /orders/:id     controllers.OrderController.editOrder(id: String)
+nocsrf
DELETE /orders/:id     controllers.OrderController.deleteOrder(id: String)